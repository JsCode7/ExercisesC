#include <stdio.h>

int main(){
	int x, num1 = 3, num2 = 5;
	printf("Ecuacion a resolver (x + %d = %d) \n", num1, num2);
	x = num2 - num1;
	printf("El valor de x seria %d ya que se resta -3 a ambos lados de la ecuacion" , x);
	
}
